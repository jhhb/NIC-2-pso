To compile:

g++ pso.cpp

To run:

./a.out topology numberOfParticles numberOfIterations problemType numberOfDimensions

settings for topology - ri, vn, ra, gl

settings for problemType - rok, ack, ras



Example run:

./a.out gl 50 2000 ras 30
